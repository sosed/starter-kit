import { WeatherImgSrcPipe } from './weather-img-src.pipe';

describe('WeatherImgSrcPipe', () => {
  it('create an instance', () => {
    const pipe = new WeatherImgSrcPipe();
    expect(pipe).toBeTruthy();
  });
});
